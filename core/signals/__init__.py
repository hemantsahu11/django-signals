# default_app_config = 'signals.apps.SignalsConfig'


# we are writing this otherwise we have to change in settings file in installed apps
# like :   signals.apps.SignalsConfig    same we used to do previously

